# Environment variables for backend

# Flask Configuration
FLASK_ENV=development
DEBUG=True
PORT=5000

# CORS Configuration (update with your Vercel URL in production)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# GPU Configuration (set to "cpu" if no GPU available)
DEVICE=cuda

# Model Configuration
MODEL_CHECKPOINT_PATH=./saved_models/character_cnn.pt

# API Configuration
API_TIMEOUT=30

# Logging
LOG_LEVEL=INFO
