# RNN Frontend Environment Variables

# For local development:
# - Leave empty (uses Vite proxy) or set to http://localhost:5001
# - Vite will proxy /api/* requests to the backend

# For production (Vercel):
# - Leave empty (uses Vercel rewrite in vercel.json)
# - Vercel rewrites /api/* requests to Railway backend
REACT_APP_API_BASE=

# Example for production override (if needed):
# REACT_APP_API_BASE=https://your-railway-backend-url.railway.app
